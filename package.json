{
  "name": "dns-check1998",
  "version": "1.0.0",
  "description": "A Node.js module to dynamically check DNS information for a given domain",
  "main": "dns-check.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "icepro",
  "license": "MIT",
  "dependencies": {
    "dns": "^0.2.2"
  }
}
